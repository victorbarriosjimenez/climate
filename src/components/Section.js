import React from 'react';

function Section({ id }) {
    return (
        <section id={id}>
            {/* Add content for each section based on the id */}
        </section>
    );
}

export default Section;
